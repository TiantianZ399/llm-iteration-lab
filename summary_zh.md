# Benchmark Summary Note

This repository currently exposes a small reproducible shortest-path benchmark and its generated artifacts.

The filename is kept for backward compatibility with the existing project layout. A polished Chinese-language summary has not been finalized yet.

For the current public snapshot, use these files first:

- `README.md` for project scope and repository context
- `docs/ROADMAP.md` for planned next steps
- `scripts/run_benchmark_comparison.py` for the benchmark generation pipeline
- `tables/` and `figures/` for committed benchmark outputs
